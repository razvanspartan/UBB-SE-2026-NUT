<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TeamNut.Views.MealPlanView.MealPlanPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TeamNut.Views.MealPlanView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid Padding="20">
            <StackPanel Spacing="20" HorizontalAlignment="Center" VerticalAlignment="Top" MaxWidth="600">

                <TextBlock 
                    Text="Your Meal Plan" 
                    Style="{StaticResource TitleTextBlockStyle}" 
                    HorizontalAlignment="Center"/>

                <Button 
                    Content="Generate Meals from Pantry" 
                    Style="{StaticResource AccentButtonStyle}" 
                    HorizontalAlignment="Stretch"
                    Command="{x:Bind ViewModel.GenerateMealsFromPantryCommand}"/>

                <TextBlock Text="Generated meals will appear here." HorizontalAlignment="Center" Foreground="Gray"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>