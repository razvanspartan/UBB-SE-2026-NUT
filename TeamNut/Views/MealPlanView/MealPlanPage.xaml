<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TeamNut.Views.MealPlanView.MealPlanPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TeamNut.Views.MealPlanView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="20" MaxWidth="800">
            <Grid>
                <TextBlock 
                    Text="Your Personalized Meal Plan" 
                    Style="{StaticResource TitleTextBlockStyle}" 
                    HorizontalAlignment="Center"/>

                <Button 
                    x:Name="SettingsButton"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Background="Transparent"
                    BorderThickness="0"
                    Click="SettingsButton_Click"
                    ToolTipService.ToolTip="Update your preferences">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE713;" FontSize="16"/>
                        <TextBlock Text="Settings" FontSize="14"/>
                    </StackPanel>
                </Button>
            </Grid>

            <Button
                x:Name="RegenerateTestButton"
                HorizontalAlignment="Center"
                Click="RegenerateTestButton_Click"
                Margin="0,5,0,0">
                <StackPanel Orientation="Horizontal" Spacing="6">
                    <FontIcon Glyph="&#xE72C;" FontSize="14"/>
                    <TextBlock Text="Regenerate (Test)" FontSize="13"/>
                </StackPanel>
            </Button>

            <TextBlock 
                x:Name="GoalDescriptionText"
                HorizontalAlignment="Center" 
                FontSize="16"
                FontWeight="SemiBold"
                Foreground="{ThemeResource SystemFillColorAttentionBrush}"
                Margin="0,5,0,0"
                TextWrapping="Wrap"/>

            <TextBlock 
                x:Name="StatusMessageText"
                HorizontalAlignment="Center" 
                Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                FontWeight="SemiBold"
                Margin="0,10,0,0"
                TextWrapping="Wrap"/>

            <TextBlock 
                x:Name="TotalNutritionText"
                HorizontalAlignment="Center" 
                FontSize="14"
                FontWeight="SemiBold"
                Foreground="{ThemeResource SystemFillColorSuccessBrush}"
                Margin="0,5,0,0"
                TextWrapping="Wrap"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" Margin="0,20,0,0">
            <StackPanel MaxWidth="800" Spacing="15">
                <ListView 
                    x:Name="MealsListView"
                    SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid 
                                Padding="15"
                                Margin="0,5"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Spacing="8">
                                    <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                                        <TextBlock 
                                            Text="{Binding Name}"
                                            FontSize="18"
                                            FontWeight="SemiBold"/>
                                        <Border
                                            Background="{ThemeResource AccentFillColorSecondaryBrush}"
                                            CornerRadius="4"
                                            Padding="8,2"
                                            VerticalAlignment="Center">
                                            <TextBlock Text="{Binding MealType}" FontSize="12" FontWeight="SemiBold"/>
                                        </Border>
                                    </StackPanel>

                                    <TextBlock 
                                        Text="{Binding Description}"
                                        Opacity="0.7"
                                        TextWrapping="Wrap"
                                        MaxWidth="500"/>

                                    <StackPanel Orientation="Horizontal" Spacing="15">
                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <TextBlock FontWeight="SemiBold">
                                                <Run Text="{Binding Calories}"/>
                                                <Run Text="kcal" FontWeight="Normal"/>
                                            </TextBlock>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <TextBlock>
                                                <Run Text="{Binding Protein}"/>
                                                <Run Text="g protein"/>
                                            </TextBlock>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <TextBlock>
                                                <Run Text="{Binding Carbs}"/>
                                                <Run Text="g carbs"/>
                                            </TextBlock>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <TextBlock>
                                                <Run Text="{Binding Fat}"/>
                                                <Run Text="g fat"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,5,0,0">
                                        <Border 
                                            Background="{ThemeResource SystemFillColorSuccessBrush}"
                                            CornerRadius="4"
                                            Padding="8,4"
                                            Visibility="{Binding IsVegan}">
                                            <TextBlock Text="Vegan" FontSize="12"/>
                                        </Border>
                                        <Border 
                                            Background="{ThemeResource SystemFillColorCautionBrush}"
                                            CornerRadius="4"
                                            Padding="8,4"
                                            Visibility="{Binding IsKeto}">
                                            <TextBlock Text="Keto" FontSize="12"/>
                                        </Border>
                                        <Border 
                                            Background="{ThemeResource SystemFillColorAttentionBrush}"
                                            CornerRadius="4"
                                            Padding="8,4"
                                            Visibility="{Binding IsGlutenFree}">
                                            <TextBlock Text="Gluten-Free" FontSize="12"/>
                                        </Border>
                                        <Border 
                                            Background="{ThemeResource SystemFillColorCriticalBrush}"
                                            CornerRadius="4"
                                            Padding="8,4"
                                            Visibility="{Binding IsLactoseFree}">
                                            <TextBlock Text="Lactose-Free" FontSize="12"/>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Spacing="10" VerticalAlignment="Top">
                                    <FontIcon 
                                        Glyph="&#xE735;"
                                        FontSize="24"
                                        Foreground="Gold"
                                        HorizontalAlignment="Center"
                                        Visibility="{Binding IsFavorite}"/>

                                    <Button
                                        Content="Add to Logs"
                                        Click="AddMealToLogsButton_Click"
                                        HorizontalAlignment="Right"
                                        MinWidth="110"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Button 
                    x:Name="SaveToDailyLogButton"
                    HorizontalAlignment="Center"
                    Margin="0,20,0,20"
                    Click="SaveToDailyLogButton_Click"
                    Style="{StaticResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE74E;" FontSize="16"/>
                        <TextBlock Text="Save to Daily Log"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
