<Page
    x:Class="TeamNut.Views.NutritionistChat.NutritionistChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Messages list (UI skeleton). Items will be populated by the ViewModel in a separate task/branch -->
        <ListView x:Name="MessagesList" Grid.Row="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="0,6">
                        <TextBlock x:Name="AuthorText" FontWeight="SemiBold" />
                        <TextBlock x:Name="MessageText" TextWrapping="WrapWholeWords" />
                        <TextBlock x:Name="TimestampText" FontSize="10" Opacity="0.6" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Input area - will be wired to commands in the viewmodel task -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
            <TextBox x:Name="InputBox" HorizontalAlignment="Stretch" AcceptsReturn="False" Width="*" />
            <Button x:Name="SendButton" Content="Send" />
        </StackPanel>
    </Grid>
</Page>