<Page
    x:Class="TeamNut.Views.ShoppingListView.ShoppingListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:TeamNut.ViewModels"
    xmlns:models="using:TeamNut.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <UserControl.Resources>
        <!-- Adding some small styles for the Delete Button -->
        <Style x:Key="DeleteButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E81123"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="CornerRadius" Value="4"/>
        </Style>
    </UserControl.Resources>

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Grocery Helper" FontSize="16" Foreground="Gray" Margin="0,0,0,4"/>
            <TextBlock Text="Shopping List" FontSize="32" FontWeight="Bold"/>
        </StackPanel>

        <!-- Input Box -->
        <Border Grid.Row="1" 
                Background="{ThemeResource LayerFillColorDefaultBrush}" 
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                BorderThickness="1" 
                CornerRadius="8" 
                Padding="16" 
                Margin="0,0,0,24">
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox x:Name="NewItemTextBox" 
                         PlaceholderText="Add something to your list..." 
                         VerticalAlignment="Center" 
                         Margin="0,0,16,0"
                         KeyUp="NewItemTextBox_KeyUp"/>
                
                <Button Grid.Column="1" 
                        Content="Add to list" 
                        Style="{StaticResource AccentButtonStyle}" 
                        Click="AddButton_Click"/>
            </Grid>
        </Border>

        <!-- List View -->
        <ListView Grid.Row="2" 
                  ItemsSource="{x:Bind ViewModel.Items, Mode=OneWay}"
                  SelectionMode="None">
            
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:ShoppingItem">
                    <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16,12"
                            Margin="0,4,0,8">
                        
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Checkbox bound to IsChecked -->
                            <CheckBox Grid.Column="0" 
                                      IsChecked="{x:Bind IsChecked, Mode=TwoWay}" 
                                      VerticalAlignment="Center" 
                                      Margin="0,0,16,0"/>
                            
                            <!-- Item Text -->
                            <TextBlock Grid.Column="1" 
                                       Text="{x:Bind IngredientName, Mode=OneWay}" 
                                       VerticalAlignment="Center" 
                                       FontSize="16"
                                       TextWrapping="Wrap"/>
                            
                            <!-- Delete Component -->
                            <Button Grid.Column="2" 
                                    Content="🗑" 
                                    Style="{StaticResource DeleteButtonStyle}"
                                    Command="{Binding ElementName=RootPage, Path=ViewModel.DeleteItemCommand}" 
                                    CommandParameter="{x:Bind}" 
                                    Click="DeleteButton_Click"
                                    ToolTipService.ToolTip="Delete item"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
            
            <ListView.ItemContainerStyle>
                <!-- Remove default hovering/selection grey backgrounds since we styled our own ItemTemplate -->
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,0"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</Page>
