<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TeamNut.Views.UserView.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TeamNut.Views.UserView"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <Grid Padding="20">
            <Border 
             MaxWidth="400" 
             VerticalAlignment="Center" 
             HorizontalAlignment="Center"
             Background="{ThemeResource LayerFillColorDefaultBrush}"
             BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
             BorderThickness="1"
             CornerRadius="8"
             Padding="30">

                <StackPanel Spacing="15">
                    <TextBlock 
                     Text="Login" 
                     Style="{StaticResource TitleTextBlockStyle}" 
                     HorizontalAlignment="Center" 
                     Margin="0,0,0,10"/>

                    <TextBox 
                     Header="Username" 
                     PlaceholderText="Enter your username"
                     Text="{x:Bind ViewModel.CurrentUser.Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                    <PasswordBox 
                     Header="Password" 
                     PlaceholderText="Enter your password"
                     PasswordRevealMode="Peek"
                     PasswordChanged="PasswordInput_PasswordChanged"/>

                    <TextBlock 
                    Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay, TargetNullValue='', FallbackValue=''}"
                    Foreground="Red"
                    TextWrapping="Wrap"
                    HorizontalAlignment="Center"
                    FontSize="12"/> 

                    <Button 
                     Content="Login" 
                     Style="{StaticResource AccentButtonStyle}" 
                     HorizontalAlignment="Stretch" 
                     Margin="0,10,0,0"
                     Command="{x:Bind ViewModel.LoginCommand}"
                        />

                    <HyperlinkButton 
                     Content="Don't have an account? Create one." 
                     HorizontalAlignment="Center"
                     x:Name="CreateAccountButton"
                     Click="ToRegister_Click"
                    />
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
