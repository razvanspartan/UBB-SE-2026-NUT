
<Page
    x:Class="TeamNut.Views.RemindersView.RemindersPage"
    x:Name="RemindersRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:TeamNut.Views.RemindersView"
    xmlns:models="using:TeamNut.Models"
    mc:Ignorable="d">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                Margin="0,0,0,20" 
                Padding="15" 
                Background="{ThemeResource LayerFillColorDefaultBrush}" 
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                BorderThickness="1" 
                CornerRadius="8">
            <StackPanel Spacing="5">
                <TextBlock Text="Next Active Reminder" FontWeight="SemiBold" Foreground="{ThemeResource SystemAccentColor}"/>
                <Grid ColumnDefinitions="*, Auto">
                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="LastReminderTitle" 
                                   Text="{x:Bind ViewModel.Reminders[0].Name, Mode=OneWay, FallbackValue='No reminders scheduled'}" 
                                   FontSize="18" FontWeight="Bold"/>
                        <TextBlock Text="{x:Bind ViewModel.Reminders[0].FullDateTimeDisplay, Mode=OneWay}" 
                                   Foreground="Gray"/>
                    </StackPanel>
                    <Button Grid.Column="1" Content="View Details" VerticalAlignment="Center"
                            Command="{x:Bind ViewModel.PrepareNewReminderCommand}"/>
                </Grid>
            </StackPanel>
        </Border>

        <ListView Grid.Row="1" 
                  ItemsSource="{x:Bind ViewModel.Reminders, Mode=OneWay}"
                  SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:Reminder">
                    <Grid Padding="12" BorderThickness="0,0,0,1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="2">
                            <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" FontSize="16"/>
                            <TextBlock Text="{x:Bind FullDateTimeDisplay}" Foreground="Gray" FontSize="12"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                            <Button Content="Edit" FontSize="12"/>
                            <Button Content="Delete" 
                                    Foreground="Red" 
                                    FontSize="12"
                                    Command="{Binding ElementName=RemindersRoot, Path=ViewModel.DeleteReminderCommand}"
                                    CommandParameter="{x:Bind}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
